# The browser always calls this same-origin path. Do not put a private backend
# hostname, Tailscale IP, or secret in a VITE_* variable.
VITE_API_BASE_URL=/api/v1

# Optional local-development proxy. Production uses deploy/nginx.conf.
DEV_API_PROXY_TARGET=http://100.76.5.32:8080
